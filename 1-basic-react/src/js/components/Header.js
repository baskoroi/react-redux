import React from 'react';
import Title from './header/Title';

export default React.createClass({
    render: function() {
        return (
            <header>
                <Title />
            </header>
        );
    }
});