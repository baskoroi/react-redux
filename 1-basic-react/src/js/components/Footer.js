import React from 'react';

const Footer = React.createClass({
    render: function() {
        return (
            <footer>
                Footer!
            </footer>
        );
    }
});

export default Footer;