import React from "react";

const Header = React.createClass({
    render: function() {
        return (
            <footer>
                <p>Some footer here...</p>
            </footer>
        );
    }
});

export default Header;